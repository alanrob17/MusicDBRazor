@page
@model MusicDB.Pages.Records.DeleteModel

@{
    ViewData["Title"] = "Delete Record — " + Model.Record.Name;
    var record = Model.Record;
}



<div class="details-wrapper">
    <div class="record-card">
        <div class="record-card-header record-card-header--danger">
            <div class="record-avatar record-avatar--danger">💿</div>
            <div class="record-header-text">
                <h1>Delete @(record.Name ?? "Record")?</h1>
                <span class="record-subtitle record-subtitle--danger">⚠️ Warning: This action is permanent and cannot be undone.</span>
            </div>
        </div>

        <div class="record-card-body">
            <div class="delete-prompt-banner">
                Are you sure you want to permanently delete this record from the database?
            </div>

            <div class="detail-grid">
                <div class="detail-row">
                    <span class="detail-label">Artist</span>
                    <span class="detail-value">@(record.Artist?.Name ?? "—")</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Name</span>
                    <span class="detail-value">@(record.Name ?? "—")</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Sub Title</span>
                    <span class="detail-value @(string.IsNullOrWhiteSpace(record.SubTitle) ? "empty" : "")">@(string.IsNullOrWhiteSpace(record.SubTitle) ? "—" : record.SubTitle)</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Recorded</span>
                    <span class="detail-value @(record.Recorded == null ? "empty" : "")">@(record.Recorded?.ToString() ?? "—")</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Field</span>
                    <span class="detail-value @(string.IsNullOrWhiteSpace(record.Field) ? "empty" : "")">@(string.IsNullOrWhiteSpace(record.Field) ? "—" : record.Field)</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Length</span>
                    <span class="detail-value @(string.IsNullOrWhiteSpace(record.Length) ? "empty" : "")">@(string.IsNullOrWhiteSpace(record.Length) ? "—" : record.Length)</span>
                </div>
            </div>
        </div>

        <div class="record-card-footer">
            <form method="post">
                <input type="hidden" asp-for="Record.RecordId" />
                <button type="submit" class="btn-action btn-delete">🗑️ Confirm Delete</button>
                <a asp-page="./Index" class="btn-action btn-back">Cancel</a>
            </form>
        </div>
    </div>
</div>
